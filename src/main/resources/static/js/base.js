layui.config(
    {
        base : "js/extend/"
    }
).extend(
    {
        redirect : "redirect",
        ajax : "ajax",
        echartsTheme : "echartsTheme",
        echarts : "echarts",
        setter : "config",
        admin : "admin",
        view : "view"
    }
);